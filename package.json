{
  "name": "onetab",
  "version": "0.1.0",
  "private": true,
  "description": "指到你了 —— 收集小确幸，选不出来时闭眼一指",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "miniprogram-api-typings": "^3.12.1",
    "typescript": "^5.5.4",
    "vitest": "^1.6.0"
  }
}
